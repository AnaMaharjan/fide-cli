name: Release Fide CLI

on:
  push:
    tags:
      - "fide-cli-v*"

permissions:
  contents: write
  id-token: write
  deployments: write

jobs:
  publish-cli:
    permissions:
      contents: write
      id-token: write
      deployments: write
    uses: ChrisLally/lally-workflows/.github/workflows/npm-publish-release.yml@main
    with:
      package_manager: pnpm
      pnpm_version: "9"
      tag_prefix: "fide-cli-v"
      package_dir: "."
      install_working_directory: "."
      deployment_environment: "Preview - fide-cli"
