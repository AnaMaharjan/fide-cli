---
title: "fide store"
description: "fide store <command> [flags]"
full: true
---

import { CLICommandPageInteractive } from '@/components/cli-layout/interactive/cli-command-page-interactive';

<CLICommandPageInteractive data={{"name":"fide store","summary":"Configured statement stores and store builds","description":"fide store <command> [flags]","usage":["fide store <command> [flags]"],"arguments":[],"options":[],"outputFields":[],"subcommands":[{"name":"status","summary":"Inspect configured stores and build state","href":"./store-status"},{"name":"sql","summary":"Run SQL against a configured sqlite or postgres statement store","href":"./store-sql"},{"name":"build","summary":"Build a recipe-backed store from configured sources","href":"./store-build"}],"schemas":[],"examples":[{"command":"fide store status"},{"command":"fide store sql --store primary 'select * from statements limit 10'"},{"command":"fide store build --statements combined"}],"notes":[{"title":"Notes","body":"Statement stores are configured under `statementStores` in `settings.json`.\nQuery stores are configured under `queryStores` in `settings.json`.\n`fide-jsonl` stores are local statement directories; use `fide graph write` to author into them.\nLocal .fide authoring lives under `fide graph`; built runtime state lives under `fide store`.\nUse `fide store build --statements ...` for statement stores and `fide store build --queries ...` for query stores."}]}} />
