---
title: "fide statements load"
description: "fide statements load --graph-key <graph-key>"
full: true
---

import { CLICommandPageInteractive } from '@/components/cli-layout/interactive/cli-command-page-interactive';

<CLICommandPageInteractive data={{"name":"fide statements load","description":"fide statements load --graph-key <graph-key>","usage":["fide statements load --graph-key <graph-key>","fide statements load --graph-key <graph-key> --from-date 2026-03-01 --to-date 2026-03-31","fide statements load --graph-key <graph-key> --replace-batches","fide statements load --graph-key <graph-key> --batch-chunk-count 250"],"arguments":[],"options":[{"name":"--graph-key","type":"graph-key","description":"Graph key to load local statements into","required":true},{"name":"--from-date","type":"YYYY-MM-DD","description":"Start date for local statement batches to load","required":false},{"name":"--to-date","type":"YYYY-MM-DD","description":"End date for local statement batches to load","required":false},{"name":"--replace-batches","type":"boolean","description":"Before loading, purge graph rows listed in `sourceDraftRootPendingReplacement` (string or array) inside dated `_meta.json` (honors the date range), clear those fields, then purge statement batches that have no `_meta.json` key and no local `.jsonl` batch","required":false},{"name":"--pretty","shorthand":"-p","type":"boolean","description":"Human-readable output","required":false},{"name":"--batch-chunk-count","type":"string","required":false,"description":"Chunk size for statement-batch id dedup queries before parsing files"}],"outputFields":[],"subcommands":[],"schemas":[{"surface":"statements.load","command":"fide statements load","params":[{"name":"--graph-key","type":"string","required":true,"description":"Graph key to load local statements into"},{"name":"--from-date","type":"string","description":"Start date for local statement batches to load"},{"name":"--to-date","type":"string","description":"End date for local statement batches to load"},{"name":"--batch-chunk-count","type":"string","description":"Chunk size for statement-batch id dedup queries before parsing files"},{"name":"--replace-batches","type":"boolean","description":"Before loading, purge graph rows listed in `sourceDraftRootPendingReplacement` (string or array) inside dated `_meta.json` (honors the date range), clear those fields, then purge statement batches that have no `_meta.json` key and no local `.jsonl` batch"},{"name":"--pretty","type":"boolean","description":"Human-readable output"}],"outputFields":[],"json":"{\n  \"command\": \"fide statements load\",\n  \"params\": [\n    {\n      \"name\": \"graph-key\",\n      \"type\": \"string\",\n      \"required\": true,\n      \"description\": \"Graph key to load local statements into\"\n    },\n    {\n      \"name\": \"from-date\",\n      \"type\": \"string\",\n      \"description\": \"Start date for local statement batches to load\"\n    },\n    {\n      \"name\": \"to-date\",\n      \"type\": \"string\",\n      \"description\": \"End date for local statement batches to load\"\n    },\n    {\n      \"name\": \"batch-chunk-count\",\n      \"type\": \"string\",\n      \"description\": \"Chunk size for statement-batch id dedup queries before parsing files\"\n    },\n    {\n      \"name\": \"replace-batches\",\n      \"type\": \"boolean\",\n      \"description\": \"Before loading, purge graph rows listed in `sourceDraftRootPendingReplacement` (string or array) inside dated `_meta.json` (honors the date range), clear those fields, then purge statement batches that have no `_meta.json` key and no local `.jsonl` batch\"\n    },\n    {\n      \"name\": \"pretty\",\n      \"type\": \"boolean\",\n      \"description\": \"Human-readable output\"\n    }\n  ],\n  \"output\": {}\n}"}],"examples":[{"command":"fide statements load --graph-key primary"},{"command":"fide statements load --graph-key primary --from-date 2026-03-01 --to-date 2026-03-31"},{"command":"fide statements load --graph-key primary --replace-batches"},{"command":"fide statements load --graph-key primary --batch-chunk-count 250"}],"notes":[{"title":"Notes","body":"Loads canonical local statements from this project's `.fide/statements/` into the target graph.\nDate filters are inclusive and apply to the dated local statement batch layout.\nStatement batch ids are checked in chunks before parsing files so already-loaded batches can be skipped efficiently.\nWith `--replace-batches`, reconcile `fide statements write --replace-draft`: remove superseded statement batches (including every batch that shared the draft path), clear pending markers in `_meta.json`, purge orphaned graph batches, then ingest."}]}} />
