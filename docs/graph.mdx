---
title: "fide graph"
description: "fide graph <command> [flags]"
full: true
---

import { CLICommandPageInteractive } from '@/components/cli-layout/interactive/cli-command-page-interactive';

<CLICommandPageInteractive data={{"name":"fide graph","summary":"Graph authoring, configured store access, builds, and graph definitions","description":"fide graph <command> [flags]","usage":["fide graph <command> [flags]"],"arguments":[],"options":[],"outputFields":[],"subcommands":[{"name":"write","summary":"Write statement inputs into a local .fide directory","href":"./graph-write"},{"name":"draft","summary":"Create a markdown statement draft in a local .fide directory","href":"./graph-draft"},{"name":"status","summary":"Inspect local graph state and configured runtime status","href":"./graph-status"},{"name":"query","summary":"Run an ad hoc graph query against a configured statement store","href":"./graph-query"},{"name":"build","summary":"Build configured statement or query stores from graph sources","href":"./graph-build"},{"name":"defs","summary":"Inspect statement and entity definitions","href":"./graph-defs"}],"schemas":[],"examples":[{"command":"fide graph write '[{ ... statement inputs ... }]'"},{"command":"fide graph query write --statement-store sqlite --name recentStatements 'select * from statements limit 10'"},{"command":"fide graph status"},{"command":"fide graph query --statement-store primary 'select * from statements limit 10'"},{"command":"fide graph build --statement-store combined"},{"command":"fide graph draft --file inputs.json"},{"command":"fide graph defs"}],"notes":[{"title":"Notes","body":"Local authoring commands (`write`, `draft`) resolve `--fide-dir <path>`, `FIDE_DIR`, the nearest `.fide` directory, then the current working directory.\n`status` shows local graph status by default and can target configured runtime backends with `--statement-store` or `--query-store`.\n`query` executes ad hoc graph queries, and `query write` saves local query definitions.\nRuntime commands (`query`, `build`) operate on configured statement stores and query stores."}]}} />
