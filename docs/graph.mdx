---
title: "fide graph add"
description: "fide graph add --local [--target <path>] --in <inputs> [--format <json|jsonl|fsd>] [--no-normalize] [--json] [--draft]"
full: true
---

import { CLICommandPageInteractive } from '@/components/cli-layout/interactive/cli-command-page-interactive';

<CLICommandPageInteractive data={{"name":"fide graph add","description":"fide graph add --local [--target <path>] --in <inputs> [--format <json|jsonl|fsd>] [--no-normalize] [--json] [--draft]","usage":["fide graph add --local [--target <path>] --in <inputs> [--format <json|jsonl|fsd>] [--no-normalize] [--json] [--draft]","fide graph add --local [--target <path>] --params '<json>' [--format <json|jsonl|fsd>] [--no-normalize] [--json] [--draft]","fide graph add --local [--target <path>] --stdin [--format <json|jsonl|fsd>] [--no-normalize] [--json] [--draft]","fide graph validate --in <input> [--format <json|jsonl|fsd>] [--json]","fide graph root --in <input> [--format <json|jsonl|fsd>] [--json]","fide graph status [--target <path>]","fide graph query --sql \"<query>\" [--json] [--allow-write]"],"options":[{"name":"--allow-write","required":false},{"name":"--draft","required":false},{"name":"--format","type":"json|jsonl|fsd","required":false},{"name":"--in","type":"inputs","required":true},{"name":"--json","required":false},{"name":"--local","required":true},{"name":"--no-normalize","required":false},{"name":"--params","required":true},{"name":"--sql","required":true},{"name":"--stdin","required":true},{"name":"--target","type":"path","required":false}],"notes":[{"title":"Notes","body":"- Normalization is ON by default for `graph add`.\n- Use `--target <path>` to target a .fide directory from any directory.\n- Without `--target`, local graph commands use the current working directory unless `.fide/settings.json` provides a graphDir override.\n- `--draft` writes a statement-doc markdown file to .fide/statement-drafts/YYYY/MM/DD/<root>.md.\n- `graph add` only accepts statement inputs via `--stdin`, `--in`, or `--params`.\n- `--stdin`/`--in` can auto-detect json/jsonl/fsd, or use --format to force.\n- `validate`/`root` accept statement-doc inputs and json/jsonl batches.\n- `status` reports whether the target directory has a .fide folder."}]}} />
