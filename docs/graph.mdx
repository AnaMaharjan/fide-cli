---
title: "fide graph"
description: "fide graph <command> [flags]"
full: true
---

import { CLICommandPageInteractive } from '@/components/cli-layout/interactive/cli-command-page-interactive';

<CLICommandPageInteractive data={{"name":"fide graph","description":"fide graph <command> [flags]","usage":["fide graph <command> [flags]"],"arguments":[],"options":[],"outputFields":[],"subcommands":[{"name":"status","summary":"Inspect local graph state and configured runtime status","href":"./graph-status"},{"name":"list","summary":"List local project graphs","href":"./graph-list"},{"name":"get","summary":"Inspect one local project graph","href":"./graph-get"},{"name":"save","summary":"Project local graph metadata into a hosted workspace graph","href":"./graph-save"},{"name":"build","summary":"Build configured graphs from graph sources","href":"./graph-build"},{"name":"defs","summary":"Inspect Fide entity definitions and statement rules","href":"./graph-defs"}],"schemas":[{"surface":"graph","command":"fide graph","params":[],"outputFields":[],"json":"{\n  \"command\": \"fide graph\",\n  \"params\": [],\n  \"output\": {}\n}"}],"examples":[{"command":"fide graph list"},{"command":"fide graph save --graph combined-graph-postgres --dry-run"},{"command":"fide graph save --graph primary --type postgres"},{"command":"fide graph status"},{"command":"fide query run --graph primary 'select * from statements limit 10'"},{"command":"fide query save --graph sqlite --name recentStatements 'select * from statements limit 10'"},{"command":"fide graph build --graph combined"},{"command":"fide statements draft --name research-notes --file inputs.json"},{"command":"fide graph defs"}],"notes":[{"title":"Notes","body":"Verb model: `statements write|draft` is for local statement inputs, `query save` is for named query definitions, and `graph save` is for hosted graph metadata.\nLocal authoring commands under `statements` resolve `--fide-dir <path>`, `FIDE_DIR`, the nearest `.fide` directory, then the current working directory.\n`graph list` and `graph get` default to local project graph definitions.\n`graph save --graph <key>` will reuse the local project graph definition with the same key when available.\n`graph save` projects shared graph metadata only into the workspace bound in project settings; local connection settings stay in project `.fide/settings.json`.\n`fide start` uses the same projection model when syncing local graphs into the hosted workspace.\nHosted `graph save` accepts `--dry-run` to preview whether the write would change shared state.\nPrefer the top-level `fide query ...` and `fide statements ...` surfaces for local authoring.\nHosted graph/query commands resolve auth from the current project account or `FIDE_ACCOUNT_ID`."}]}} />
